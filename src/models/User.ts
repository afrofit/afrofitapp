export type User = {
  id: string;
  email: string;
  isVerified: boolean;
  isAdmin: boolean;
  isRegistered: boolean;
  username: string;
  joinDate: string;
  rankId: number;
  hasTrial: boolean;
};
